name: Multiple Jobs
on:
    workflow_dispatch

jobs:
    first-job:
        runs-on: ubuntu-latest

        steps:
            - run: echo "first job"
    
    second-job:
        runs-on: ubuntu-latest

        steps:
            - run: echo "second job"