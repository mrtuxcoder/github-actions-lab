name: Failure Demo
on:
    workflow_dispatch:
jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - run: echo "starting.."
            - run: exit 0
            - run: echo "done"