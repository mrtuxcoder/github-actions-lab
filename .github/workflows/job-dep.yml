name: Job Dependencies
on:
 workflow_dispatch:

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - run: echo "running test"

    build:
          needs: test

          runs-on: ubuntu-latest

          steps:
            - run: echo "building app"